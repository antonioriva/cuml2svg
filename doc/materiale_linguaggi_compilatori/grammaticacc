S → diagram DIAGRAM_TYPE diagram_name { PACKAGES_DEFINITIONS }

DIAGRAM_TYPE → uml

PACKAGES_DEFINITIONS → PACKAGE_DEFINITION PACKAGES_DEFINITIONS

PACKAGE_DEFINITION → package package_name { CLASSES_DEFINITIONS }

PACKAGES_DEFINITIONS → ε

OPTIONAL_COMMENT → ε
OPTIONAL_COMMENT → COMMENT
COMMENT → comment

CLASSES_DEFINITIONS → CLASS_DEFINITION CLASSES_DEFINITIONS

CLASSES_DEFINITIONS → ε
CLASS_DEFINITION → OPTIONAL_COMMENT VISIBILITY CLASS_TYPE class_name { RELATIONS ATTRIBUTES METHODS }

RELATIONS → relations { RELATIONS_DEFINITIONS }

RELATIONS_DEFINITIONS → RELATION_DEFINITION RELATIONS_DEFINITIONS

RELATIONS_DEFINITIONS → ε

RELATION_DEFINITION → OPTIONAL_COMMENT RELATION_TYPE class_name RELATION_CARDINALITY CLASSES_LIST_WITH_CARDINALITY

RELATION_TYPE → use
RELATION_TYPE → extend
RELATION_TYPE → associate
RELATION_TYPE → include
RELATION_TYPE → composed
RELATION_TYPE → realize
RELATION_TYPE → depend

CLASSES_LIST_WITH_CARDINALITY → , class_name RELATION_CARDINALITY CLASSES_LIST_WITH_CARDINALITY

CLASSES_LIST_WITH_CARDINALITY → ε

RELATION_CARDINALITY → ε
RELATION_CARDINALITY → (CARDINALITY, CARDINALITY, CARDINALITY)

CARDINALITY → string
CARDINALITY → ε

ATTRIBUTES → ATTRIBUTE ATTRIBUTES
ATTRIBUTES → ε
ATTRIBUTE → COMMENT VISIBILITY ATTRIBUTE_TYPE attribute_name DEFAULT_VALUE

DEFAULT_VALUE → ε
DEFAULT_VALUE → = value

METHODS → METHOD METHODS
METHODS → ε 
METHOD → COMMENT VISIBILITY METHOD_TYPE method_name { METHOD_ARGS }
METHOD_ARGS → METHOD_ARG METHOD_ARGS_ITERATION
METHOD_ARGS_ITERATION → , METHOD_ARG METHOD_ARGS_ITERATION
METHOD_ARGS_ITERATION → ε 
METHOD_ARG → COMMENT ARG_TYPE arg_name DEFAULT_VALUE

CLASS_TYPE → interface
CLASS_TYPE → class

METHOD_TYPE → TYPE
ARG_TYPE → TYPE
ATTRIBUTE_TYPE → TYPE
TYPE → string
TYPE → ε 

VISIBILITY → private
VISIBILITY → public
VISIBILITY → string
VISIBILITY → ε 